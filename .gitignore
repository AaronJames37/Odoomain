.env
.env.local
__pycache__/
*.pyc
*.pyo
*.pyd
.DS_Store
postgres_data/
odoo_data/
config/
# Only ignore a stray compose file at the repo root; docker/ is tracked.
/docker-compose.yml
node_modules/

# Licensed Odoo Enterprise source — copied from the server, never committed.
enterprise-addons/

# Bundle pulled from the VPS: db dump, filestore and licensed enterprise source.
bundle/

# Local database dumps / filestore snapshots
*.dump
*.bundle
filestore-*.tar.gz
.pytest_cache/
*.egg-info/
dist/
build/
*.sqlite3
